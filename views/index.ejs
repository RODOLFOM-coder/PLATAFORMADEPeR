<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Guia Perguntas</title>
<link rel="stylesheet" href="/css/style.css">

</head>
<body>
    <h1>Guia Perguntas</h1>
    <hr>
    <h3>Lista de perguntas</h3>  

    <p>Nome:<%= nome%></p>
    <p>Linguagem:<%= lang%></p>
    <p>Site:<%= site%></p>

    <% if(msg == false) { %>
        <h3>nenhum erro!!!!!!!!</h3>
        <% } else{ %>
            <h3>nenhum erro!!!!!!!!</h3>
            Isso é uma mensagem de erro</h3>
            <% } %>

<!-- // só coloco igual quando quero exibir variaveis -->
<% produtos.forEach(function(produto) { %>
<hr>
<h4><%= produto.nome %></h4>
<h5><%= produto.preco %></h5>
<% }) %>

</body>
</html>